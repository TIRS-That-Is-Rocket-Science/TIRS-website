---
import PageLayout from '@/layouts/PageLayout.astro';
import { t } from '@/lib/i18n';

const locale = 'en';
const text = t(locale);
---

<PageLayout title={text.aboutTitle} locale={locale}>
  <h1>{text.aboutHeading}</h1>
  <p>{text.aboutIntro}</p>
</PageLayout>
